# Store-admin custom app credentials (Settings → Apps → Develop apps).
# Only a shpat_ Admin API token can create products — theme-access (shptka_)
# and app-automation tokens cannot.
SHOPIFY_STORE=your-store.myshopify.com
SHOPIFY_ADMIN_ACCESS_TOKEN=shpat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
